{
  "name": "backend",
  "version": "1.0.0",
  "description": "this is the backend for the recipe app it hold all the server side logic for the app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ifeanyi f. Okwuobi",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.10"
  }
}
